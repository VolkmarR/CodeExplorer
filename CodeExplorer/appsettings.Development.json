{
  "//Urls": "Development only, so the web UI's dev-server proxy has a port to name. It must not be in the base file: a container would then bind loopback and answer nothing.",
  "Urls": "http://localhost:5080"
}
