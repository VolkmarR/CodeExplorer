/**
 * The app's version, replaced at build time by Vite from `package.json` (`vite.config.ts`). A
 * constant rather than something the server answers with: it names the bundle the browser is
 * running, which is the thing a bug report needs, and asking the API for it would say what the
 * server is instead.
 */
declare const APP_VERSION: string
